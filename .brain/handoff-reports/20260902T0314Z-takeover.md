# 部分接管 — Boss Brain

结论：部分接管，1 项真实环境验证 BLOCKED；仓库内未发现既有 `.brain/`、`HANDOFF.md`、`TASKS.md` 或 `HANDOFF_ACCEPTANCE.md`，因此无法按完整项目契约宣称接管成功。

## 仓库核对

- 工作树：接手时干净，分支 `main`。
- 最新提交：`b349726 feat: add task drift and continuity checks`。
- 远程：`origin` 指向项目 GitHub 仓库；本次尚未提交或推送。

## 验收结果

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 本地用户旅程与韧性测试 | PASS | `python3 -m unittest -q tests.test_boss tests.test_resilience tests.test_user_journeys`，53/53 |
| Python 编译 | PASS | `python3 -m py_compile plugins/boss-brain/scripts/boss.py scripts/install.py` |
| Shell 语法 | PASS | `bash -n install.sh uninstall.sh tests/remote_codex_e2e.sh` |
| diff 检查 | PASS | `git diff --check` |
| host-84 SSH | PASS | 地址可达，可执行远端命令 |
| host-84 Codex 版本 | PASS（发现漂移） | 实际为 `codex-cli 0.151.0`，不是交接提示中的 0.152 |
| host-84 真实 Codex E2E | BLOCKED | host-84 实际为 Codex 0.151.0；加入 `--skip-git-repo-check`、隔离 PATH、`--ignore-user-config`、`mcp_servers={}` 和每-turn timeout 后，Hook/安装可启动，但远端仍报告未授权 Cloudflare MCP，且 fixture 响应存在非确定性 marker，完整 alias/resume/Stop 审计未完成 |
| host-84 GitHub CLI | PASS | 已通过 SSH + sudo 安装 `gh` 2.45.0 |
| host-84 GitHub Vault 授权 | PARTIAL | `service:github` 配置不可用；`service:github-actions` token 可调用 GitHub API，但标准 `gh auth login` 缺少 `read:org` |
| host-84 GitHub Canary | PASS | 使用临时 `GH_TOKEN` 完成 Boss `machine init --create-remote --push`，确认仓库创建与 push 后删除临时仓库；代码已补齐 Git credential helper |
| host-84 systemd 基础能力 | PASS | `default.target` 与 `basic.target` 均 active；`degraded` 来自既有 `mp-release-watch` 和 `suqu-evals-machine` 失败，不属于 Boss Brain |
| host-84 多日 systemd Canary | NOT_RUN | 需要单独的非生产 Boss remote 和持续观察窗口；不应通过清除其他服务 failed 状态伪造绿色结果 |
| host-84 临时机器恢复 Canary | PASS | 临时 HOME + 临时 bare remote 完成 Machine Brain init、push、clean HOME clone/restore，恢复项目存在；未触碰真实项目 |
| host-84 timer unit dry-run | PASS | 临时 HOME 下 `boss machine timer-install --interval 5 --dry-run` 成功生成 unit 路径 |

## 本次实现

- 为无稳定任务 ID 的 legacy `TASKS.md` 增加保守的自然语言目标漂移提示；只注入 pointer-only 上下文并在 explain trace 中标为 `goal-drift`。
- 为 Wiki 检查增加基于 token Jaccard 重叠的人工合并建议；不自动合并或覆盖内容。
- 为 conventions 支持显式 `conflicts-with:` 诊断；冲突使检查失败，并给出需人工裁决的策略。
- 修正远程 E2E 脚本在隔离 PATH 下找不到 `CODEX_BIN` 的入口缺陷。
- 更新覆盖率与测试边界文档，并补充用户旅程测试。

## 未验证与风险

- 真实 GitHub 仓库创建、长周期 systemd timer、真实回收机器恢复：未运行，仅有 fake/local 测试证据。
- 语义 Wiki 合并仍是建议，不是自动演化；目标漂移依赖保守 token 重叠，不声称理解任意自然语言。
- 远程 E2E 仍需在 host-84 Codex 0.151 环境完成 alias 会话诊断后重新执行。
- Claude Code、macOS、Windows 按项目决策不属于本次验收范围。
- GitHub token 曾在交互式 SSH 调试中发生终端回显，未写入仓库或远端配置；应由凭据持有人尽快轮换该 Vault 条目。后续不再使用该 token。
