# 2026-08-06

## v0.3.8 三道闸 + 文件结构改良
用户复盘四个问题:命名待定、brain 写入收口无人管、dev-log 无结构、wiki 零产出。
核心诊断(用户侧 agent 自述):"被强制的必然完成,靠自觉的必然遗漏"。
改法:evidence 强制 wiki 判断字段(三类标准,none 表示判断过);Stop hook 从一道闸
变三道(证据新鲜度/wiki 必答/.brain 收口);dev-log 定型为按日期文件夹;LESSONS 概念
并入 wiki。顺带修了证据 commit 后误拦的竞态(EV_TS 计入 .brain 最近 commit 时间)。
doctor 5 个 stop-hook 用例全 PASS。

## v0.3.10 三源考古
用户复盘 TripStar init 漏掉大量会话知识(职责边界/学习进度/被拒方案),且换机无密钥交接通道。brain-init 重写:三源考古+分类落盘+覆盖自检,vault push 成为 init 内建步骤。

## 体检模式增量补齐
重复执行 brain-init 从"仅汇报"升级为按覆盖度清单差距检查+经同意补缺,幂等安全。

## init 范围参数
/brain-init 10 = 接入+对最近10条提交深度补账,组合复用 backfill,不新造提取逻辑。

## wiki 深度许可+首篇深度词条
五段定为下限;command-carrier-tradeoff 千字成文,含被否方案(自带CLI/revert双形态)与否决理由。

## v0.4.0 文档质量体系
用户以 Suqu 交接提示词为基准指出约束太瘦。骨架(SKILL)与质量标准(guides/四本)分离,动笔才整篇加载;验收四态纪律+接管成功硬前提接入 takeover/handoff。

## handoff 会话清扫
交接是会话知识的末班车:/handoff 增加九类会话考古必做步骤,与 brain-init 复用同一标准。

## 首次实弹+8处歧义修复
subagent 用纯协议对 sop-ui 出 HANDOFF,质量达标;最值钱的产出是执行者视角的 8 条协议吐槽,当日全修(v0.4.1)。
