# 关键知识同步机制

用户要求在 boss-brain 独立优化，不将插件开发纳入 TripStar 控制项目。实现 `d8d1306` 增加会话级待核实记录：Prompt 纠正候选、Agent 显式 flag、后续 Prompt/Stop 提醒、文档变更证据与 updated/deferred/dismissed 处置。Hook 不保存原话或自动写业务文档，遵守禁写和 session modes。

`python3 -m unittest tests.test_boss tests.test_resilience tests.test_user_journeys tests.test_knowledge_sync` 60 tests OK；py_compile 和 diff --check 通过。通过原安装器更新本机，codex/claude 均报告 fallback，安装副本 knowledge list 可读到已处置案例。当前会话热加载与宿主 UI Stop 提醒未作真实登录 E2E，不宣称已验证。

实际案例以显式 Agent discovery 方式登记 TripStar 控制仓库的通道关系，修正权威文档后通过文件摘要证据关闭。限制见 docs/knowledge-sync.md：只识别候选，人工/Agent 核实事实；并非跨会话自动知识归纳器。
