# Continuity protocol v2

## Delivered

- `89b2780`: versioned global directive management; project Brain initialization and
  document mapping; multi-project task bindings and bounded dynamic context; durable
  knowledge review metadata; unified brain-init skill and installation entrypoints.
- `35b23d7`: preserve native Hook trust tables and remove partially migrated fallback
  entries. Initial local upgrade caught a real migration failure; installation was
  paused, only missing table headers were restored, regression added and reinstall verified.
- `e10243e`: project initialization occurs through confirmed work bindings, never a
  lexical prompt match. A question about fixing code cannot authorize Brain writes.
- `8677bbc`: hosted CI now discovers the v2 regression suite and compiles its runtime.

Each implementation commit was immediately pushed. The source repository is public;
only generic code, synthetic fixtures and sanitized mechanism evidence are committed.

## Verification

The deterministic suite passed 77 tests. Plugin and both Skill validators, Python
compilation and diff checks passed. Real Codex acceptance in an isolated temporary HOME
proved global directive loading, two-project context, a retained constraint after resume,
tool-free retrieval and lifecycle traces. Its optional knowledge fixture proved a confirmed
decision was saved and its review resolved using changed-file evidence, without a business
commit or external publication. Temporary fixture credentials were not emitted or committed.

Local installation selected Codex native plugin and Claude fallback; repeated global
initialization reported unchanged. Duplicate Codex fallback hooks were removed. Other
projects were only previewed, not bulk-initialized; inaccessible/ignored entries remain
explicit migration gaps rather than silently changing repository policy.

## Limits

No claim of automatic semantic truth validation, zero task drift, real Claude/platform
acceptance or fleet-wide rollout. Revised host skill catalogs should be picked up in a
new thread. Agent documentation writes and Git persistence remain separately verified.

## Urgent live-session Hook repair

The preceding acceptance missed an active session retaining a pruned plugin-cache
path. The user hit a missing-file Stop failure. `1e695fd` restores executable-only
compatibility entries, remembers cache versions during installation, and makes
missing-script/interpreter commands skip rather than block. The user's final scope
was missing-resource compatibility, not project enable/disable switches.

All 84 tests passed, including seven focused regression cases. Installed the repaired native plugin and
Claude fallback using the normal cachebuster/installer flow; both retained old Stop
entrypoints returned `{}` with exit zero. Installed native guards also returned `{}`
with missing paths. No business projects were modified. Windows command behavior
remains unverified on a real host.

## Multi-project quality refinement

`93ff028` followed the original v2 plan without modifying business projects. New
regressions first reproduced missing scope replacement, healthy-context loss from
one damaged registered project, premature global rule writes for unknown project
selection, and an initialization failure aborting the remaining project results.

Implemented explicit task project/access replacement while preserving other tasks
and historical work audits; isolated metadata failures and reused capability
inventory per context render; rejected unknown initialization targets before global
rule writes; reported per-project partial failures and refused non-object manifests
without overwriting them. Updated the skill with the exact scope-correction behavior.

Verification: 92 deterministic tests, skill/plugin validators, compile and diff
checks passed. Extended real isolated Codex acceptance passed global rules,
multi-project/resume, corrected scope, damaged-project isolation and authorized
knowledge persistence without a business commit. Source, stable distribution and
native cache matched after installation. All three retained old Stop entrypoints
returned `{}` successfully. Implementation was immediately pushed. No fleet-wide
rollout or real Claude/Windows acceptance is claimed.

## Original-rule and legacy-behavior preservation repair

`9aa7f9d` addressed the user's correction that core requirements were weakened in
the v2 migration. Compared the original twelve clauses and pre-v2 `c5bf077` against
the enabled v2 path. The detailed R01–R12 findings are maintained in
`docs/requirements-preservation-audit.md`; this log does not duplicate that inventory.

Restored the twelve original clauses verbatim and added a missing-core rejection
gate. Reconnected task/goal drift, roster, task-to-audit links, metadata-first context
packing and mapped diagnostic readers; restored explain preview; removed silent
knowledge queue truncation and skipped a missing internal Hook runtime module.
Clarified the inherited distinction between fact evidence and user authority.

Validation: 108 deterministic tests passed, including sixteen preservation tests.
Native (not fallback) isolated Codex acceptance passed long-state marker retrieval,
multi-project/resume/scope correction, damaged-project isolation, original core-rule
retrieval without tools and authorized knowledge saving. Plugin/skill validators,
compile and diff checks passed. Installed runtime/cache matched source; both global
directive files contained the complete core; four old Stop entries returned `{}`.
Code commit was immediately pushed. No business projects were initialized or edited.

Remaining differences are explicit: legacy mandatory receipts and automatic startup
patrol/machine initialization are not restored pending scope/silence reconciliation;
candidate guidance retrieval is intentionally multi-project. No all-environment or
perfect Agent-obedience claim. No requirements-interrogation mode was added.

## Forward-compatible receipt repair (source acceptance only)

The user rejected a forced rollback tradeoff: retain new capabilities while repairing
old behavior. `9946caf` reconnects visible project receipts to enabled v2. Task owners
and reference projects have separate labels; off/changes/always remain available.
Receipt delivery is independent of context-body deduplication, fits the context budget,
does not grant write authority, and yields to strict user output requirements.

Added seven deterministic receipt cases and an optional real native Codex assertion
on the final answer. All 115 tests passed. Final source native acceptance passed
visible receipts, core rules, long multi-project context, resume, corrected scope and
damaged-project isolation. The first native receipt run also passed knowledge
writeback; the final run followed the small related-guidance receipt refinement.
Plugin validation, compilation and diff checks passed. Code was immediately pushed.

README/observability now distinguish legacy startup behavior, repaired source and
the still-unchanged shared installation. The compatibility audit lists remaining
gates, including startup patrol/machine initialization, enabled-v2 strict remediation,
and platform acceptance. No shared plugin reinstall, global-rule edit, business
project change, data deletion or rollback occurred. Source acceptance is not deployment.
