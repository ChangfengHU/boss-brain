
## wiki-sync 写路径
中央同步脚本入插件:防泄闸/离线队列/哈希去重;踩坑两处——CF 1010 拦 python UA(加自定义 UA 解),source 不 export 子进程读不到。

## wiki/spec 边界(v0.5.1)
用户纠正 wiki 范围做太广。红线:wiki 只收无状态跨项目通用知识(唯一同步中央库),项目专属规范/设计语言/代码约定归 spec/。加归位判定树。

## 收敛单目录(v0.5.2)
弃用 spec + 协作/技术双目录。唯一区分线:进不进wiki。项目专属规矩全归 conventions/,由 HANDOFF 指向。
